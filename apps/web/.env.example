# Copy to .env.local (gitignored) for local Google Workspace mode.
VITE_STORAGE_MODE=google
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# Optional: Google Cloud API key for Picker (not the OAuth client id)
# VITE_GOOGLE_API_KEY=
