# D08 DXF named value extended

Replace `{{attdef_slot}}` (ATTDEF), `{{note}}` (MULTILEADER),
`{{dim_label}}` (DIMENSION override), and `{{label}}` (ACAD_TABLE cell)
placeholders in a loaded DXF template using `replace_named_value`.
